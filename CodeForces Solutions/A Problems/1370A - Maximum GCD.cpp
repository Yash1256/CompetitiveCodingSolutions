#include <bits/stdc++.h>
using namespace std;

// STM :keep revisiting
// to get maximum gcd the one number can be x and other be x*2
// below n one such pair psbl is n and n/2

int main() {
    int tc;
    cin >> tc;

    while (tc--) {
        int n;
        cin >> n;

        cout << n / 2 << "\n";
    }

    return 0;
}